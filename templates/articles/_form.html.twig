{% form_theme form 'bootstrap_4_layout.html.twig' %}

{{ form_start(form) }}
    {{ form_widget(form) }}
    <br><br>
    <button class="btn">{{ button_label|default('Sauvegardez') }}</button>
{{ form_end(form) }}


